PATCH FIX BUILD VERCEL - V18 MULTI SOAL

Isi patch ini hanya 3 file yang berubah:
1. apps/siswa/lib/question-scoring.ts
2. apps/siswa/app/api/attempts/[id]/submit/route.ts
3. apps/siswa/app/pembahasan/page.tsx

Cara pakai paling mudah:
1. Extract ZIP ini.
2. Copy folder apps ke root project Anda.
3. Pilih replace/overwrite file lama.
4. Jalankan:
   npm run build
5. Kalau build aman, push ke GitHub:
   git add .
   git commit -m "fix scoring type build"
   git push

Catatan:
Patch ini memperbaiki error TypeScript:
Argument of type 'JsonValue' is not assignable to parameter of type 'AnswerValue'.

Patch ini tidak mengubah database dan tidak perlu db push ulang hanya untuk fix build ini.
